public class Main
{
    public static void main(String[] args)
    {
        int summerCost = 30;
        int winterCost = 50;
        int fallCost = 20;
        int springCost = 40;
        int yearTotal = 0;

        yearTotal = summerCost + winterCost + fallCost + springCost;
        System.out.println("The maintenance cost for each season is as follows: summer: " + summerCost + " winter: " + winterCost + " fall: " + fallCost + " spring: " + springCost +
                " and the yearly maintenance cost is: " + yearTotal);




















    }
}